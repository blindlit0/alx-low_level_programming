#include "main.h"
/**
 *mul - multiply two numbers
 *@a: first  number to be multiplied
 *@b: number to be multiplied against first
 *Return: the product of first and second number
 */

int mul(int a, int b)
{
	return (a * b);
}
