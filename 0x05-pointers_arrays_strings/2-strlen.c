#include "main.h"

/**
 * _strlen - counts the length of a string and returns value
 * @s: string
 * Return: Length of string
 */

int _strlen(char *s)
{
	int string;

	for (string = 0; s[string] != '\0' ; string++)

	return (string);
}
